name: Final owner app verification

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/build-owner-app-final.yml'
  workflow_dispatch:

permissions:
  contents: write
  actions: read

concurrency:
  group: final-owner-app-verification
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 50

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Restore the existing update key
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          curl -fsSL \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H 'Accept: application/vnd.github+json' \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "${GITHUB_API_URL}/repos/${GITHUB_REPOSITORY}/actions/artifacts?name=Crimescene-Owner-App-final&per_page=100" \
            > /tmp/artifacts.json

          artifact_id=$(jq -r '[.artifacts[] | select(.expired == false)] | sort_by(.created_at) | reverse | .[0].id // empty' /tmp/artifacts.json)
          test -n "$artifact_id"

          curl -fsSL --retry 5 --retry-all-errors -L \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H 'Accept: application/vnd.github+json' \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "${GITHUB_API_URL}/repos/${GITHUB_REPOSITORY}/actions/artifacts/${artifact_id}/zip" \
            -o /tmp/previous-owner-app.zip

          mkdir -p /tmp/previous-owner-app
          unzip -q /tmp/previous-owner-app.zip -d /tmp/previous-owner-app
          key_file=$(find /tmp/previous-owner-app -type f -name 'Crimescene-Owner-Update-Key.jks' -print -quit)
          password_file=$(find /tmp/previous-owner-app -type f -name 'Crimescene-Owner-Update-Key-password.txt' -print -quit)
          signature_file=$(find /tmp/previous-owner-app -type f -name 'Crimescene-Owner-App-signature.txt' -print -quit)
          test -s "$key_file"
          test -s "$password_file"

          cp "$key_file" /tmp/Crimescene-Owner-Update-Key.jks
          password=$(tr -d '\r\n' < "$password_file")
          test ${#password} -ge 20
          printf '%s\n' \
            'storeFile=/tmp/Crimescene-Owner-Update-Key.jks' \
            "storePassword=${password}" \
            'keyAlias=crimescene-owner' \
            "keyPassword=${password}" \
            > android-owner-app/signing.properties

          if [ -n "$signature_file" ] && [ -s "$signature_file" ]; then
            cp "$signature_file" /tmp/previous-signature.txt
          fi

      - name: Check customer-facing wording and live service
        shell: bash
        run: |
          set -euo pipefail
          grep -q 'APP_VERSION = "1.2.0"' android-owner-app/app/src/main/java/com/crimesceneplay/owner/AppConfig.java
          grep -q 'versionCode 4' android-owner-app/app/build.gradle
          grep -q 'versionName "1.2.0"' android-owner-app/app/build.gradle
          grep -q '크라임씬플레이 서면점' android-owner-app/app/src/main/java/com/crimesceneplay/owner/MainActivity.java
          grep -q 'PendingIntent.getForegroundService' android-owner-app/app/src/main/java/com/crimesceneplay/owner/NotificationSyncService.java
          grep -q 'cachedKey' android-owner-app/app/src/main/java/com/crimesceneplay/owner/SecurePrefs.java

          if grep -R -n -E '서면1호점|·|ChatGPT|OpenAI|인공지능|AI가|AI로|디버그|토큰|API 키|Supabase' \
            android-owner-app/app/src/main android-owner-app/README.md; then
            echo '사용자 화면 문구 검사를 통과하지 못했습니다.' >&2
            exit 1
          fi

          api='https://jhjbiejqtbidloxcwryr.supabase.co/functions/v1/owner-app-api'
          key='sb_publishable_mA5DOfPA-ExloawT3aJpNw_2PeVgEEc'
          curl -fsSL --retry 8 --retry-all-errors -H "apikey: ${key}" "${api}/health" > /tmp/health.json
          grep -q '"version":"1.2.0"' /tmp/health.json
          grep -q '"realtime":"LONG_POLL"' /tmp/health.json
          code=$(curl -sS -o /tmp/unauthorized.json -w '%{http_code}' -H "apikey: ${key}" "${api}/wait?after=0")
          test "$code" = '401'
          grep -q '다시 연결' /tmp/unauthorized.json

      - name: Install Android build tools
        shell: bash
        run: |
          set -euo pipefail
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager 'platforms;android-34' 'build-tools;34.0.0'

      - name: Lint and build signed release
        shell: bash
        run: |
          set -euo pipefail
          gradle -p android-owner-app --no-daemon clean :app:lintRelease :app:assembleRelease
          test -s android-owner-app/app/build/outputs/apk/release/app-release.apk
          test -s android-owner-app/app/build/reports/lint-results-release.html

      - name: Verify APK identity permissions and update compatibility
        shell: bash
        run: |
          set -euo pipefail
          apk='android-owner-app/app/build/outputs/apk/release/app-release.apk'
          tools="$ANDROID_HOME/build-tools/34.0.0"

          "$tools/apksigner" verify --verbose --print-certs "$apk" | tee /tmp/signature.txt
          "$tools/aapt" dump badging "$apk" | tee /tmp/package.txt
          "$tools/aapt" dump permissions "$apk" | tee /tmp/permissions.txt
          unzip -t "$apk" > /tmp/zip-test.txt

          grep -q "package: name='com.crimesceneplay.owner' versionCode='4' versionName='1.2.0'" /tmp/package.txt
          grep -q "application-label:'크라임씬플레이 예약 알림'" /tmp/package.txt
          grep -q 'android.permission.INTERNET' /tmp/permissions.txt
          grep -q 'android.permission.POST_NOTIFICATIONS' /tmp/permissions.txt
          grep -q 'android.permission.RECEIVE_BOOT_COMPLETED' /tmp/permissions.txt
          grep -q 'android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING' /tmp/permissions.txt

          if grep -q -E 'READ_CONTACTS|READ_SMS|SEND_SMS|RECORD_AUDIO|ACCESS_FINE_LOCATION|CAMERA' /tmp/permissions.txt; then
            echo '필요하지 않은 권한이 포함되어 있습니다.' >&2
            exit 1
          fi

          if [ -s /tmp/previous-signature.txt ]; then
            previous=$(grep -m1 'certificate SHA-256 digest:' /tmp/previous-signature.txt | cut -d: -f2- | xargs)
            current=$(grep -m1 'certificate SHA-256 digest:' /tmp/signature.txt | cut -d: -f2- | xargs)
            test -n "$previous"
            test "$previous" = "$current"
          fi

      - name: Launch on Android 14 and inspect first-use screen
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          arch: x86_64
          target: default
          profile: pixel_6
          disable-animations: true
          emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          script: |
            adb install -r "$GITHUB_WORKSPACE/android-owner-app/app/build/outputs/apk/release/app-release.apk"
            adb shell am force-stop com.crimesceneplay.owner
            adb shell am start -W -n com.crimesceneplay.owner/.MainActivity | tee /tmp/activity-start.txt
            grep -q 'Status: ok' /tmp/activity-start.txt
            sleep 3
            adb shell uiautomator dump /sdcard/owner-window.xml
            adb pull /sdcard/owner-window.xml /tmp/owner-window.xml
            grep -q '예약 알림 연결' /tmp/owner-window.xml
            grep -q '관리자 암호' /tmp/owner-window.xml
            grep -q '연결하기' /tmp/owner-window.xml
            ! adb logcat -d -t 700 | grep -q 'FATAL EXCEPTION'
            adb exec-out screencap -p > /tmp/first-use-screen.png
            test -s /tmp/first-use-screen.png

      - name: Prepare delivery bundle
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/delivery android-owner-app/dist
          apk='android-owner-app/app/build/outputs/apk/release/app-release.apk'
          cp "$apk" /tmp/delivery/Crimescene-Owner-App.apk
          cp "$apk" android-owner-app/dist/Crimescene-Owner-App.apk
          cp /tmp/signature.txt /tmp/delivery/Crimescene-Owner-App-signature.txt
          cp /tmp/package.txt /tmp/delivery/Crimescene-Owner-App-package.txt
          cp /tmp/permissions.txt /tmp/delivery/Crimescene-Owner-App-permissions.txt
          cp /tmp/first-use-screen.png /tmp/delivery/Crimescene-Owner-App-screen.png
          cp android-owner-app/app/build/reports/lint-results-release.html /tmp/delivery/Crimescene-Owner-App-lint.html

          printf '%s\n' \
            '크라임씬플레이 예약 알림 설치 방법' \
            '' \
            '1. Crimescene-Owner-App.apk 파일을 휴대폰에서 엽니다.' \
            '2. 휴대폰에서 설치를 허용합니다.' \
            '3. 앱을 열고 관리자 페이지에서 사용하는 암호를 입력합니다.' \
            '4. 알림을 허용합니다.' \
            '' \
            '연결은 처음 한 번만 하면 됩니다. 이후 새 예약과 예약 취소가 휴대폰으로 옵니다.' \
            > /tmp/delivery/설치방법.txt

          (cd /tmp/delivery && sha256sum Crimescene-Owner-App.apk > Crimescene-Owner-App.apk.sha256)
          cp /tmp/delivery/Crimescene-Owner-App.apk.sha256 android-owner-app/dist/
          cp /tmp/signature.txt android-owner-app/dist/Crimescene-Owner-App-signature.txt

      - uses: actions/upload-artifact@v4
        with:
          name: Crimescene-Owner-App-1.2.0-final
          path: /tmp/delivery
          if-no-files-found: error
          retention-days: 90

      - name: Publish verified build
        shell: bash
        run: |
          set -euo pipefail
          printf '%s\n' \
            '# 크라임씬플레이 예약 알림 1.2.0 검증 결과' \
            '' \
            '- 결과: 성공' \
            '- 앱 버전: 1.2.0' \
            '- 패키지: com.crimesceneplay.owner' \
            '- 사용자 화면 문구 검사: 성공' \
            '- 서버 연결 검사: 성공' \
            '- 코드 검사와 릴리스 빌드: 성공' \
            '- APK 서명과 업데이트 호환성 검사: 성공' \
            '- Android 14 설치와 실행 검사: 성공' \
            '- 첫 사용 화면 확인: 성공' \
            "- GitHub Actions 실행 번호: ${GITHUB_RUN_ID}" \
            "- 실행 시각: $(date -u '+%Y-%m-%dT%H:%M:%SZ')" \
            > android-owner-app/BUILD_RESULT.md

          rm -f android-owner-app/signing.properties
          rm -rf android-owner-app/.gradle android-owner-app/app/build
          rm -f .github/workflows/build-owner-app.yml .github/workflows/build-owner-app-final.yml

          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A .github/workflows android-owner-app/BUILD_RESULT.md android-owner-app/dist
          git commit -m 'build: publish verified owner app 1.2.0'
          git pull --rebase origin main
          git push origin HEAD:main
