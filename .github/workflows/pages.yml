name: Admin deployment maintenance

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Admin deployment is being upgraded."
